# Vite public base
# Use relative URLs so nginx proxy works correctly
# VITE_API_URL is not set - defaults to '/api' in components

# Optional: set public base if served from subpath
# VITE_BASE_URL=/
